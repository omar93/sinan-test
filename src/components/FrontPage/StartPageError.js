import React from "react";

function StartPageError() {
    return (
        <>
            <h2>
                Sorry, the server can't be reached right now. Try to reload the
                page or try again later
            </h2>
            ;
        </>
    );
}

export default StartPageError;
